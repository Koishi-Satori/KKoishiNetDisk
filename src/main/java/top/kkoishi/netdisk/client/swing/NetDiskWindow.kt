package top.kkoishi.netdisk.client.swing

import top.kkoishi.netdisk.client.Context
import javax.swing.JFrame

class NetDiskWindow(private val context: Context, title: String): JFrame(title) {
    fun run() {
        TODO()
    }
}